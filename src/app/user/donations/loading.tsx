// 사용자 후원 지갑 화면의 라우트 로딩 상태를 렌더링합니다.
import { UserDonationLoadingState } from "@/components/donations/user-donation-loading-state";

export default function UserDonationsLoading() {
  return <UserDonationLoadingState />;
}
